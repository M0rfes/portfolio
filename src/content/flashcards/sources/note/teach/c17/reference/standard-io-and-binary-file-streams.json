{
  "source": {
    "type": "note",
    "id": "teach/c17/reference/standard-io-and-binary-file-streams",
    "title": "Standard I/O & Binary File Streams in C17",
    "href": "/notes/teach/c17/reference/standard-io-and-binary-file-streams/",
    "tags": [
      "teach/c17",
      "reference",
      "stdio",
      "file-io"
    ]
  },
  "cards": [
    {
      "id": "note:teach/c17/reference/standard-io-and-binary-file-streams:0",
      "question": "What are the two distinct return conventions used by C standard library I/O functions?",
      "answer": "Status/error codes where 0 means success and non-zero means failure (e.g. fseek, fclose), and value/count/handle returns where valid pointers/counts mean success and NULL/EOF/-1 indicates failure (e.g. fopen, fread, ftell)."
    },
    {
      "id": "note:teach/c17/reference/standard-io-and-binary-file-streams:1",
      "question": "Why does fread(ptr, size, nmemb, stream) take both size and nmemb as separate parameters instead of a single total byte count?",
      "answer": "It enables structured record reading where fread() returns the count of complete records transferred, making truncated multi-byte struct reads immediately detectable."
    },
    {
      "id": "note:teach/c17/reference/standard-io-and-binary-file-streams:2",
      "question": "What does ftell() return if an error occurs while determining file stream position?",
      "answer": "It returns -1L and sets errno."
    },
    {
      "id": "note:teach/c17/reference/standard-io-and-binary-file-streams:3",
      "question": "What is rewind(stream) functionally equivalent to in C standard I/O?",
      "answer": "It is equivalent to (void)fseek(stream, 0L, SEEK_SET) except that it also clears the stream error indicator."
    }
  ]
}
